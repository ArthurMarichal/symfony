{% extends 'base.html.twig' %}

{% block title %}New Quack{% endblock %}

{% block body %}
    <h1>Create new Quack</h1>
    <div class="duck-text">
        {{ include('quack/_form.html.twig') }}
    </div>
    <a href="{{ path('quack_index') }}"><button class="btn btn-warning">back to list</button></a>
{% endblock %}
