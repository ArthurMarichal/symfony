{% extends 'base.html.twig' %}

{% block title %}Quack{% endblock %}

{% block body %}
    <h1>{{ duck.firstname }}</h1>

    <table class="table">
        <tbody>
        <tr>
            <td><img src="{{ duck.picture }}" alt="profil pic" width="250" height="250"></td>
        </tr>
            <tr>
                <th>Id</th>
                <td>{{ duck.id }}</td>
            </tr>
            <tr>
                <th>email</th>
                <td>{{ duck.email }}</td>
            </tr>
            <tr>
                <th>firstname</th>
                <td>{{ duck.firstname }}</td>
            </tr>
            <tr>
                <th>lastname</th>
                <td>{{ duck.lastname }}</td>
            </tr>
            <tr>
                <th>duckname</th>
                <td>{{ duck.duckname }}</td>
            </tr>
            <tr>
                <th>password</th>
                <td>{{ duck.password }}</td>
            </tr>
            <tr>
                <th>Quack :</th>
                <th>{{ include('quack/index.html.twig') }}</th>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('duck_index') }}">Back to list</a><br>
    <a href="{{ path('duck_edit', {'id': duck.id}) }}">Edit</a><br>
    <a href="{{ path('quack_new') }}"> Write a new Quack</a>
{#    <a href="{{ path('quack_edit', {'id': quack.id}) }}">Edit a Quack</a>#}


    {{ include('security/_delete_form.html.twig') }}
{% endblock %}
