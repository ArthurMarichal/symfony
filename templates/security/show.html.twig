{% extends 'base.html.twig' %}

{% block title %}Quack{% endblock %}

{% block body %}
    <h1>{{ duck.firstname }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ duck.id }}</td>
            </tr>
            <tr>
                <th>email</th>
                <td>{{ duck.email }}</td>
            </tr>
            <tr>
                <th>firstname</th>
                <td>{{ duck.firstname }}</td>
            </tr>
            <tr>
                <th>lastname</th>
                <td>{{ duck.lastname }}</td>
            </tr>
            <tr>
                <th>duckname</th>
                <td>{{ duck.duckname }}</td>
            </tr>
            <tr>
                <th>password</th>
                <td>{{ duck.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('duck_index') }}">back to list</a>

    <a href="{{ path('duck_edit', {'id': duck.id}) }}">edit</a>

    {{ include('security/_delete_form.html.twig') }}
{% endblock %}
